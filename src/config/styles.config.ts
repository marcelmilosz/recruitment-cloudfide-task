// Styles configuration for the application for the tailwind

export const stylesConfig = {
    borderRadius: {
        full: "rounded-full",
        parent: "rounded-[16px]",
        child: "rounded-[10px]",
        orphan: "rounded-[4px]", // for small things
    },
    animation: {
        default_duration: "duration-200",
    },
    border: {
        default: "border-2 border-border",
        default_dashed: "border-2 border-dashed border-border",
    },
};
